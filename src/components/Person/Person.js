import React from 'react';

const person = (props) => {
    return (
        <div>
            <h1>react app</h1>
            <p>My name is {props.name}</p>
        </div>
    );
};

export default person;
